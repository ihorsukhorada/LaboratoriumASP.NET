@model IEnumerable<Zaliczenie.Models.CustomerOrderItemViewModel>

@{
    ViewData["Title"] = "Customer Orders";
}

@foreach (var item in Model)
{
    <div>
        @item.OrderId
    </div>
    <div>
        @item.OrderDate
    </div>
}
